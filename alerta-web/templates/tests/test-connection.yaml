apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "alerta-web.fullname" . }}-test-connection"
  labels:
    {{- include "alerta-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "alerta-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
