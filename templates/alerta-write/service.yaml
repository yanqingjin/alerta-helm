apiVersion: v1
kind: Service
metadata:
  name: {{ include "alerta-helm.fullname" . }}-write
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ include "alerta-helm.fullname" . }}-write
    {{- include "alerta-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.server.write.service.type }}
  ports:
    - name: {{ include "alerta-helm.fullname" . }}-write
      port: {{ .Values.server.write.service.port }}
      targetPort: {{ .Values.server.write.service.port }}
      protocol: TCP
  selector:
    app: {{ include "alerta-helm.fullname" . }}-write
    {{- include "alerta-helm.selectorLabels" . | nindent 4 }}
