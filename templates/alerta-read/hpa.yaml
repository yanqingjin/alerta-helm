{{- if .Values.server.read.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "alerta-helm.fullname" . }}-read
  labels:
    {{- include "alerta-helm.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "alerta-helm.fullname" . }}-read
  minReplicas: {{ .Values.server.read.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.server.read.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.server.read.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.server.read.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.server.read.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.server.read.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
