apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "alerta-helm.fullname" . }}-test-connection"
  labels:
    {{- include "alerta-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "alerta-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
