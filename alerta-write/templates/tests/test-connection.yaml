apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "alerta-write.fullname" . }}-test-connection"
  labels:
    {{- include "alerta-write.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "alerta-write.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
