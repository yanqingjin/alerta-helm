apiVersion: v1
kind: Service
metadata:
  name: {{ include "alerta-write.fullname" . }}
  namespace: {{ include "alerta-write.namespace" . }}
  labels:
    app: {{ include "alerta-write.fullname" . }}
    {{- include "alerta-write.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: {{ include "alerta-write.fullname" . }}
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
  selector:
    app: {{ include "alerta-write.fullname" . }}
    {{- include "alerta-write.selectorLabels" . | nindent 4 }}
