apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "alerta-read.fullname" . }}-test-connection"
  labels:
    {{- include "alerta-read.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "alerta-read.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
